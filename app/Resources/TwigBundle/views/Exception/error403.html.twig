{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="genus-name">Access Denied !!!</h2>
    <div class="cd-container">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            {# ... #}
        {% endif %}

        <p>
            Sorry!!! You don't have right access permission to access this page. Checkout for any URL
            misspelling or <a href="{{ path('homepage') }}">return to the homepage</a>.
        </p>
    </div>
{% endblock %}